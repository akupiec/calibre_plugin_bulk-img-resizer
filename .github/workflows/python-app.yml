name: Python application

on:
  push:

permissions:
  contents: read

jobs:

  lint:
    runs-on: ubuntu-latest
    steps:
    - name: Run commitlint
      uses: wagoid/commitlint-github-action@v6

  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4

    - name: Set up Python 3.12
      uses: actions/setup-python@v3
      with:
        python-version: "3.12"
    - name: Run Tests
      run: |
        python -m unittest test_plugin.py

  release:
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')
    permissions:
      contents: write
    needs: [build]
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Generate Changelog
        uses: TriPSs/conventional-changelog-action@v5
        id: changelog
        with:
          github-token: ${{ secrets.github_token }}
          skip-commit: "true"
          skip-git-pull: "true"
          output-file: "false"

      - name: Build
        run: |
          echo ${{ steps.changelog.outputs.clean_changelog }} > Release.txt
          echo ${{ steps.changelog.outputs.tag }} > Release.txt

      - name: Test
        run: |
          cat Release.txt

      - name: Release
        uses: softprops/action-gh-release@v2
        with:
          body_path: ${{ steps.changelog.outputs.clean_changelog }}
          files: Release.txt